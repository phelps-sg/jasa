parent.0 = client.params
exch.id = Crete
stat.file = crete.stat

# We need to give this island its own unique random number seed
seed.0 = 1001

# We also need to give this island its own unique client port.  Actually
# if you're running on different machines, then each island can use the
# same port.  But since we don't know this ahead of time, it's safer to
# give 'em different ports
exch.client-port = 9001
